# Should Axtract be started at all ?
SERVICE_ENABLED="false"

# These variables define which services to start and stop, and in which
# order that should be done. You should not need to change anything else.

# Variables for a Lab install (axtract + northbound-lab package)
# See Admin Guide for instructions for a production install
SUBSERVICES_START="rsyslog mongod ax.rrddb cron redis-server beanstalkd zeo apache2 ax.eventd ax.start_gui ax.start_nbi"
SUBSERVICES_STOP="ax.start_nbi ax.start_gui ax.eventd apache2 zeo beanstalkd redis-server cron ax.rrddb mongod rsyslog"